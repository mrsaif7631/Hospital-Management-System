<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://unpkg.com/tailwindcss@^2/dist/tailwind.min.css" rel="stylesheet">
</head>

<body>
    {{>header}}


    <section class="text-gray-600 body-font">
        <div class="container px-5 py-24 mx-auto flex flex-wrap items-center">

            <div class="md:w-1/2  bg-gray-100 p-8 rounded mx-auto">
                <h1 class="title-font  mb-4 text-white bg-purple-500 py-2 px-8 rounded font-bold text-2xl">Organization Profile</h1>
                <h1 class="title-font font-medium text-3xl text-gray-900">{{name}}</h1>
                <p class="leading-relaxed mt-4">Email : {{email}}</p>
                <p class="leading-relaxed mt-4">Location : {{address}}</p>
                <p class="leading-relaxed mt-4">Created on : {{createdAt}}</p>
            </div>

            <div class="flex flex-nowrap my-24 mx-auto">
                <div class="lg:w-2/6 md:w-1/2  flex flex-col  bg-gray-100 rounded-lg p-8">
                    <h2 class="text-gray-900 text-lg font-medium title-font mb-5">Update Name</h2>
                    <form action="/updateme" method="post">
                        <div class="relative mb-4">
                            <input type="text" name="name" placeholder="Name"
                                class="w-full bg-white rounded border border-gray-300 focus:border-purple-500 focus:ring-2 focus:ring-purple-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                        </div>
                        <button
                            class="text-white bg-purple-500 border-0 py-2 px-8 focus:outline-none hover:bg-purple-600 rounded text-lg">Update</button>
                    </form>
                </div>


                <div class="lg:w-2/6 md:w-1/2  flex flex-col  bg-gray-100 rounded-lg p-8 mx-8">
                    <h2 class="text-gray-900 text-lg font-medium title-font mb-5">Update Address</h2>
                    <form action="/updateme" method="post">
                        <div class="relative mb-4">
                            <input type="text" name="address" placeholder="Location"
                                class="w-full bg-white rounded border border-gray-300 focus:border-purple-500 focus:ring-2 focus:ring-purple-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                        </div>
                        <button
                            class="text-white bg-purple-500 border-0 py-2 px-8 focus:outline-none hover:bg-purple-600 rounded text-lg">Update</button>
                    </form>
                </div>


                <div class="lg:w-2/6 md:w-1/2  flex flex-col  bg-gray-100 rounded-lg p-8">
                    <h2 class="text-gray-900 text-lg font-medium title-font mb-5">Update Password</h2>
                    <form action="/updateme" method="post">
                        <div class="relative mb-4">
                            <input type="text" name="password" placeholder="Password"
                                class="w-full bg-white rounded border border-gray-300 focus:border-purple-500 focus:ring-2 focus:ring-purple-200 text-base outline-none text-gray-700 py-1 px-3 leading-8 transition-colors duration-200 ease-in-out">
                        </div>
                        <button
                            class="text-white bg-purple-500 border-0 py-2 px-8 focus:outline-none hover:bg-purple-600 rounded text-lg">Update</button>
                    </form>
                </div>
            </div>
        </div>
    </section>

    {{>footer}}
    <script src="/js/app.js"></script>
</body>

</html>